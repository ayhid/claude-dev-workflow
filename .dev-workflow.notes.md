## 2026-08-17 — #24

sync takes its evidence only from pull requests, so anything pushed straight to main is invisible to it (#19). The reconcile workflow runs on merge, which is why a hand-pushed fix still needs its ticket moved by hand.

## 2026-08-28 — #19

sync no longer takes its evidence only from pull requests: it also reads commit subjects on the base branch, so work pushed straight to main reconciles (#19). This supersedes the 2026-08-17 note above. The remaining gap is latency, not blindness — reconcile.yml runs on merge and weekly, so a hand-pushed fix moves on the next scheduled run rather than immediately.
