## 2026-08-17 — #24

sync takes its evidence only from pull requests, so anything pushed straight to main is invisible to it (#19). The reconcile workflow runs on merge, which is why a hand-pushed fix still needs its ticket moved by hand.
